# Optional advanced Diun YAML. The default stack does not mount this —
# Diun is configured via environment variables in docker-compose.yml.
# See ../diun.yml.example

watch:
  workers: 20
  schedule: "0 */6 * * *"
  firstCheckNotif: false

providers:
  docker:
    watchStopped: false
    watchByDefault: true
